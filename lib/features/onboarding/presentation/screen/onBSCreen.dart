import 'package:flutter/cupertino.dart';

import '../../../../core/utils/text_styles.dart';

class Onbscreen extends StatelessWidget {
  const Onbscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child:
      Text("screen 2 ",style: Styles.body,),
    );
  }
}
